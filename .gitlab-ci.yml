pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp README.md public/
    - cp README-es.md public/
    - cp -r assets public/
  artifacts:
    paths:
      - public
  only:
    - master
